# Orvpass Locked Architecture

The architecture is locked around a Rust shared core, native platform clients, browser extensions, and encrypted synchronization.

Implementation bugs are fixed within these boundaries.
