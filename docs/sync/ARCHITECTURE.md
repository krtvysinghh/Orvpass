# Sync Architecture

Principles:

- Client encrypts everything
- Server never sees passwords
- Offline first
- Conflict safe
- Multiple devices supported

Sync modes:

1. Local only
2. LAN sync
3. Self hosted server
4. Optional hosted sync
